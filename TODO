# Old
- Improve performance with optional caching
- Fix recording for raw assets (.js, .bin, fonts...)
- Add option to serve static assets more simply (with default static/ folder?) (and record as such)
